# Phase 7 — Polish & Ship

**Goal:** Final cleanup: demo reset, print styles, port fixes, submission report update.
**Time:** 1.5 hours
**Depends on:** Phases 1-2 minimum, ideally 3-5 also done

---

## Tasks

### 7.1 — Demo reset button (if Phase 6 walkthrough not built)
- [ ] If the walkthrough wasn't built, add a standalone reset:
  - `POST /demo/reset` endpoint in `backend/app/routes/demo.py`
  - "Reset Demo" button in NavBar or footer
  - Confirmation dialog: "Reset all data to the Cedar Heights scenario?"
  - On reset: delete DB, re-seed, refresh dashboard
- [ ] If Phase 6 was built, this endpoint already exists — just add the button to NavBar

### 7.2 — Print styles for Impairment Packet
- [ ] Add `@media print` CSS to `frontend/src/index.css`:
  - Hide NavBar, back button, print button (`.no-print { display: none }`)
  - Remove shadows and rounded corners
  - Full-width layout
  - Page breaks between sections
  - Footer: "Generated by ImpairmentOS" + timestamp
- [ ] Test: Ctrl+P on the packet page should produce a clean document

### 7.3 — Print styles for Records Export (if Phase 3 done)
- [ ] Same treatment for the Records Export page
- [ ] Each impairment packet starts on a new page
- [ ] Cover page with property info and date range

### 7.4 — Port / startup consistency
- [ ] Verify `frontend/src/api.ts` BASE matches actual backend port (currently 8001)
- [ ] Backend starts on 8001: check `uvicorn` command in start script
- [ ] Note: CORS in `backend/app/config.py` already includes `localhost:5173` — this is correct (CORS lists frontend origins, not backend)
- [ ] Update or create `start.bat`:
  1. Start backend on port 8001
  2. Start frontend dev server (port 5173)
  3. Open browser to `http://localhost:5173`
  4. Seed database if empty (happens automatically on backend startup)

### 7.5 — Update SUBMISSION_REPORT.md
- [ ] Update feature descriptions with what was actually built
- [ ] Add specific testing instructions:
  - How to see enforcement blocking (click Take Action on Cedar Heights, try to close)
  - How to use records export (if built)
  - How to see property overview (if built)
  - How to run the walkthrough (if built)
- [ ] Keep it concise — evaluators said "not a dissertation"

### 7.6 — Final smoke test
- [ ] Delete `impairmentos.db`, run start script, verify:
  - [ ] Dashboard loads with correct impairment counts
  - [ ] Cedar Heights shows violations / BLOCKED badge (Phase 2)
  - [ ] Dunmoor shows active, no violations
  - [ ] Take Action on Cedar Heights -> wizard opens
  - [ ] Try to close -> violations appear
  - [ ] Complete AHJ notification + main drain test -> closure succeeds
  - [ ] View generated packet -> all compliance items COMPLETE
  - [ ] Export records works (if Phase 3 done)
  - [ ] Properties overview works (if Phase 4 done)
  - [ ] Demo reset works (if Phase 6/7.1 done)
  - [ ] No console errors in browser
  - [ ] No server errors on startup
